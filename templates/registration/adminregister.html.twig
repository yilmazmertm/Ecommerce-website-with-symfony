<!doctype html>
<html lang="en">
<head>
    <!-- Required meta tags -->
    {%  include ('admin/head.html.twig') %}
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Login</title>
    <!-- Bootstrap CSS -->

    <style>
        html,
        body {
            height: 100%;
        }

        body {
            display: -ms-flexbox;
            display: flex;
            -ms-flex-align: center;
            align-items: center;
            padding-top: 40px;
            padding-bottom: 40px;
        }
    </style>
</head>
<body>
<!-- ============================================================== -->
<!-- login page  -->
<!-- ============================================================== -->
<div class="splash-container " >
    <div class="card " >
        <div class="card-header text-center"><a href="../index.html"><img class="logo-img" src="{{ asset('assets/admin/images/logo.png') }} " alt="logo"></a><span class="splash-description">Please enter your user information.</span></div>
        <div class="card-body">
            <h1>Register</h1>

    {{ form_start(registrationForm) }}

            {{ form_widget(registrationForm) }}
            <div class="col-md-2">
                <button class="btn btn-primary">{{ button_label|default('Register') }}</button>
            </div>


    {{ form_end(registrationForm) }}


</body>
</html>


