<div class="nav-left-sidebar sidebar-dark">
      <div class="menu-list">
          <nav class="navbar navbar-expand-lg navbar-light">
              <a class="d-xl-none d-lg-none" href="#">Dashboard</a>
              <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarNav">
                  <ul class="navbar-nav flex-column">
                      <li class="nav-divider">
                          Menu
                      </li>

                      <li class="nav-item">
                          <a class="nav-link" href="{{path('admin_category_index')}}">Category</a>
                      </li>
                      <li class="nav-item">
                          <a class="nav-link" href="{{ path('admin_product_index')}}">Products</a>
                      </li>
                      <li class="nav-item">
                          <a class="nav-link" href="{{ path('admin_setting_index') }}">Settings</a>
                      </li>
                      <li class="nav-item">
                          <a class="nav-link" href="{{ path('admin_user_index') }}">Users</a>
                      </li>
                      <li class="nav-item">
                          <a class="nav-link" href="{{ path('admin_messages_index') }}">Contact Form Messages</a>
                      </li>
                      <li class="nav-item">
                          <a class="nav-link" href="{{ path('admin_comment_index') }}">User Comments</a>
                      </li>
                      <li class="nav-item dropdown nav-user">
                          <a class="nav-link nav-user-img" href="#" id="navbarDropdownMenuLink2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><img height="20" src="{{asset('uploads/images/'~ app.user.image)}}" alt=" class="user-avatar-md rounded-circle"></a>
                          <div class="" aria-labelledby="navbarDropdownMenuLink2">
                              <div class="nav-user-info">
                                  <h5 class="mb-0 text-white nav-user-name">{{ app.user.name }} {{ app.user.surname }}</h5>
                                  <span class="status"></span><span class="ml-2">Available</span>
                              </div>
                              <a class="dropdown-item" href="{{ path('admin_user_new') }}"><i class="fas fa-user mr-2"></i>Account</a>
                              <a class="dropdown-item" href="{{ path('admin_setting_index') }}"><i class="fas fa-cog mr-2"></i>Setting</a>
                              <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="fas fa-power-off mr-2"></i>Logout</a>
                          </div>
                      </li>

                  </ul>
              </div>
          </nav>
      </div>
  </div>
  <!-- ============================================================== -->
  <!-- end left sidebar -->
  <!-- ============================================================== -->
  <!-- ============================================================== -->
  
